# Web port http://localhost:8080
server.port=8080

#database source
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3305/mysql?serverTimezone=GMT&useUnicode=true&characterEncoding=utf8&useSSL=true
spring.datasource.username=root
spring.datasource.password=Wu314159265358uW

#  mybatis mapper config
mybatis.mapper-locations= classpath:mappers/*.xml

#############################redis jedis#############################
spring.redis.database=0
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=
spring.redis.pool.max-active=200
spring.redis.pool.max-wait=-1
spring.redis.pool.max-idle=8
spring.redis.pool.min-idle=0
spring.redis.timeout=0
spring.session.store-type=none

### RocketMQ ###
# # Windows start RocketMQ command
# # 1. CMD type: mqnamesrv (Don't close the window)
# # 2. start another CMD window input: mqbroker -n 127.0.0.1:9876 autoCreateTopicEnable=true (don't close window)

rocketmq.name-server=localhost:9876
rocketmq.producer.group=my-group